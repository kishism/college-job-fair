<!-- # Prevent directory listing
Options -Indexes

# Block specific sensitive PHP files and config files
<FilesMatch "^(auth_check\.php|composer\.json|composer\.lock|composer\.phar|config-select\.php|config_registry\.json|dashboard\.php|error\.php|phpinfo\.php)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# (Optional) Block direct access to files in /configs directory
<Directory "configs">
    Order Deny,Allow
    Deny from all
</Directory>

# Block vendor folder (Composer dependencies)
RewriteEngine On
RewriteRule ^vendor/ - [F,L] -->
